# Redis Message Bus Configuration for PerpsTrader
# Isolated from system Redis (port 6379)

# Connection
REDIS_HOST=127.0.0.1
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_DB=0

# Cache Database (DB 1)
REDIS_CACHE_DB=1
REDIS_CACHE_PREFIX=perps:cache:

# Job Queue Database (DB 2)
REDIS_QUEUE_DB=2

# Worker Configuration
NEWS_WORKER_CONCURRENCY=10
OPENROUTER_CONCURRENCY=8
